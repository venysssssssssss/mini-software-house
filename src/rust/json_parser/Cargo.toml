[package]
name = "json_parser"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1.0"
libc = "0.2"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = 3
lto = true
strip = true
